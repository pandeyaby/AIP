services:
  - type: web
    name: ai-industrial-portfolio
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        value: 8765
